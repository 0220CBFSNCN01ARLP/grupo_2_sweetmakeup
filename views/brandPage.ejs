<!DOCTYPE html>
<html lang="en">

<head>
    <%- include("partials/head") %>
    <link
        href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,400;0,500;0,700;0,900;1,400;1,500;1,700;1,900&display=swap"
        rel="stylesheet">

    <link rel="stylesheet" href="/css/brandPage.css" />
    <title>Make.u |
        <%= brand.name %>
    </title>
</head>

<body>
    <%- include('partials/header') %>

    <div class="brand-banner-title">
        <img src="/img/bannerBrand2.jpg" alt="Banner principal" class=" banner-img w-100 " />
        <p class="p-subtitle-section">Estas viendo productos de</p>
        <h1 class="upper-title-section"><%= brand.name %></h1>
    </div>

    <main class="container mt-1">
        <div class="row vh-75">
            <% if (brandProducts.length > 0) { %>
            <% for( let i = 0; i < brandProducts.length; i++ ) { %>
            <%- include("partials/productCard", {product: brandProducts[i]})  %>
            <% } %>
            <% } else { %>
            <p>Todavía no existen productos para esta marca</p>
            <% } %>

        </div>


    </main>
    <%- include("partials/footer") %>
</body>

</html>